version: 2

model
  -name: dim_customers
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
  - name: dim_employees
    columns:
      - name: employee_id
        tests:
          - unique
          - not_null